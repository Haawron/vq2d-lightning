# @package _global_

# usage: python train.py +experiment=cls_token_score

model:
  enable_cls_token_score: true
  cls_norm: false
  cls_scaling_type: 'softmax'
  cls_scaling: 50
  cls_learnable_scaling: false
  # late_reduce: true

# batch_size: 4