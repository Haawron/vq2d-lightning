# @package _global_

# usage: python train.py +experiment=cls_token_score

model:
  enable_cls_token_score: true
  cls_norm: false
  cls_scaling: 50
  cls_scaling_type: 'softmax'
  # late_reduce: true

# batch_size: 4